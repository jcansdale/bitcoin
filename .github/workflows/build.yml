on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - run: ./autogen.sh

    - run: ./configure --disable-wallet

    - run: make

    - run: make install # optional
